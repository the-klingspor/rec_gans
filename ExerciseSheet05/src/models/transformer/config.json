{
	"general": {
		"device": "CPU"
	},

	"training": {
		"save_model": true,
		"epochs": 200,
		"learning_rate": 1e-3,
		"scheduler": true
	},

	"testing": {
		"teacher_forcing_steps": 30,
		"closed_loop_steps": 100
	},

	"dataset": {
		"name": "chapter1"
	},

	"model": {
		"name": "example_model_name",
		"d_one_hot": 45,
		"n_heads": 8,
		"d_model": 128,
		"linear_layer_size": 512,
		"num_blocks": 4
	}
}
