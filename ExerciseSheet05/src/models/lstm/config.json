{
	"general": {
		"device": "CPU"
	},

	"training": {
		"save_model": true,
		"_epochs": 2000,
		"epochs": 200,
		"_learning_rate": 0.0001,
		"learning_rate": 0.001
	},

	"testing": {
		"teacher_forcing_steps": 30,
		"closed_loop_steps": 100
	},

	"dataset": {
		"name": "chapter1"
	},

	"model": {
		"name": "example_model_name",
		"d_one_hot": 45,
		"d_lstm": 128,
		"num_lstm_layers": 2
	}
}
